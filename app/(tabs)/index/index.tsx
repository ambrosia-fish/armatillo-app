// This file ensures the Home tab acts as the true index of the app
// It forwards to the main Home screen component defined in app/(tabs)/index.tsx

import Home from '../index';

// Re-export the Home component as the default
export default Home;