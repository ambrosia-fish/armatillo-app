export { default as SettingsScreen } from './screens/SettingsScreen/SettingsScreen';
import SettingsScreen from './screens/SettingsScreen/SettingsScreen';

// Export settings feature as default
const Settings = {
  SettingsScreen
};

export default Settings;
